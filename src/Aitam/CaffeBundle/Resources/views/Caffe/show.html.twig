<h1>Caffe</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Imgvideo1</th>
            <td>{{ entity.imgvideo1 }}</td>
        </tr>
        <tr>
            <th>Embedvideo1</th>
            <td>{{ entity.embedvideo1 }}</td>
        </tr>
        <tr>
            <th>Autore</th>
            <td>{{ entity.autore }}</td>
        </tr>
        <tr>
            <th>Descrizione</th>
            <td>{{ entity.descrizione }}</td>
        </tr>
        <tr>
            <th>Titolo</th>
            <td>{{ entity.titolo }}</td>
        </tr>
        <tr>
            <th>Created</th>
            <td>{{ entity.created|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Updated</th>
            <td>{{ entity.updated|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Images1</th>
            <td>{{ entity.images1 }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('caffe') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('caffe_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('caffe_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
